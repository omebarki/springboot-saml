server:
  ssl:
    key-store: classpath:saml/samlKeystore.jks
    # The password used to generate the certificate
    key-store-password: nalle123
    # The alias mapped to the certificate
    key-alias: omarserver

security:
  require-ssl: true

authentication:
  saml:
    idp:
      metadata-url: http://keycloak-omarprj.192.168.99.100.nip.io/auth/realms/keycloak-springboot-realm/protocol/saml/descriptor
    sp:
      entity-id: com:omar:sp
      auth:
        login-success-url: /landing
        login-failure-url: /error
        logout-success-url: /


#
#logs
#
logging:
  level:
    org:
      springframework:
        security:
          saml: DEBUG
      opensaml: DEBUG
    com:
      vdenotaris:
        spring:
          boot:
            security:
              saml: DEBUG